{
  "name": "lit",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "packages/lit/src",
  "tags": ["scope:ui"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "vite build", "cwd": "packages/lit" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "vitest run", "cwd": "packages/lit" }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "eslint packages/lit/src" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc --noEmit -p packages/lit/tsconfig.json" }
    },
    "storybook": {
      "executor": "nx:run-commands",
      "options": { "command": "storybook dev -p 6010", "cwd": "packages/lit" }
    },
    "build-storybook": {
      "executor": "nx:run-commands",
      "options": { "command": "storybook build", "cwd": "packages/lit" }
    }
  }
}
