{
  "name": "mcp",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "packages/mcp/src",
  "tags": ["scope:tooling"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "vite build", "cwd": "packages/mcp" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "vitest run", "cwd": "packages/mcp" }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "eslint packages/mcp/src" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p packages/mcp/tsconfig.json"
      }
    }
  }
}
