import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Core/Localizers" />

# Localizers

Big Calendar stores all dates as **plain text strings** in the
[RFC 3339](https://datatracker.ietf.org/doc/html/rfc3339) format — something
like `"2026-07-03T14:30:00Z"`. A string on its own is just characters though.
The calendar needs something that can _understand_ that string: add a week to it,
compare two of them, figure out which time zone offset applies, and turn it into
a human-readable label like "2:30 PM".

That something is the **localizer**.

Think of the localizer as a translator. On one side it speaks "plain date
strings." On the other side it speaks a date library (or the browser's built-in
Temporal API). You plug a localizer into `createCalendarStore` and every view,
every event, and every interaction goes through it — the rest of the calendar
never touches raw date math directly.

```
Your app                      Big Calendar core
──────────     ──────────────────────────────────────────────────────
"2026-07-03"  →  localizer  →  "add 7 days"  →  "2026-07-10"
                              "format for display" →  "Thu, Jul 3"
```

**A localizer is required.** There is no default. You must create one and pass
it when setting up the calendar.

---

## The two built-in localizers

Big Calendar ships two ready-made localizers. Both implement the same contract,
so your calendar code works identically no matter which one you choose.

### `@big-calendar/localizer-temporal` (recommended)

This localizer is built on the
[Temporal API](https://tc39.es/proposal-temporal/docs/) — the modern date and
time standard coming to the web platform. Temporal was designed specifically to
fix the historical problems with JavaScript's `Date` object (time zone handling,
arithmetic edge cases, immutability), so it is the most correct and
future-proof option.

**When to use it:** When starting a new project, or when you have no existing
date library preference. It will be the right choice for most projects.

**One thing to know:** Temporal is not yet in every browser, so this localizer
automatically loads a polyfill when it is needed. That polyfill load is
asynchronous, which is why the factory function is `async`.

#### Installation

```bash
pnpm add @big-calendar/localizer-temporal
# or: npm install @big-calendar/localizer-temporal
```

#### Basic usage

```ts
import { createTemporalLocalizer } from '@big-calendar/localizer-temporal'
import { createCalendarStore } from '@big-calendar/core'

// `await` because the polyfill may need to load first
const localizer = await createTemporalLocalizer({
  locale: 'en-US',
  timeZone: 'America/New_York',
})

const store = createCalendarStore({ localizer, /* …other options */ })
```

---

### `@big-calendar/localizer-luxon`

This localizer is built on [Luxon](https://moment.github.io/luxon/), a popular
JavaScript date library. It is a great choice if Luxon is already part of your
project — your team already knows it, it is in your bundle anyway, and you can
reuse the same library in your own code and in the calendar.

**When to use it:** When your project is already using Luxon.

**One thing to know:** Luxon is a `peerDependency`, which means _you_ install
it alongside this package. The factory is synchronous — no `await` needed.

#### Installation

```bash
pnpm add @big-calendar/localizer-luxon luxon
# or: npm install @big-calendar/localizer-luxon luxon
```

#### Basic usage

```ts
import { createLuxonLocalizer } from '@big-calendar/localizer-luxon'
import { createCalendarStore } from '@big-calendar/core'

// synchronous — no await
const localizer = createLuxonLocalizer({
  locale: 'en-US',
  timeZone: 'America/New_York',
})

const store = createCalendarStore({ localizer, /* …other options */ })
```

---

## Configuration options

Both localizers accept the same set of options. None are required — the
localizer will pick sensible defaults from your browser environment — but most
real apps will want to set at least `locale` and `timeZone` explicitly so that
the calendar renders consistently for every user, regardless of where they are.

### `locale`

A [BCP 47](https://www.ietf.org/rfc/bcp/bcp47.txt) language tag, or an
`Intl.Locale` object.

This controls how dates and times are formatted for display: which language,
which calendar system, whether months come before or after days, and so on.

```ts
// A BCP 47 string — the most common form
createLuxonLocalizer({ locale: 'en-US' })

// French as spoken in France
createLuxonLocalizer({ locale: 'fr-FR' })

// English with a 24-hour clock preference baked in via a Unicode extension tag
createLuxonLocalizer({ locale: 'en-US-u-hc-h23' })

// An Intl.Locale object — useful when you've already constructed one elsewhere
// in your app, or when you want to set options programmatically
const myLocale = new Intl.Locale('en-US', { hourCycle: 'h23' })
createLuxonLocalizer({ locale: myLocale })
```

**Default:** the locale reported by the user's browser (`Intl.DateTimeFormat().resolvedOptions().locale`).

---

### `timeZone`

An [IANA time zone](https://www.iana.org/time-zones) name — the kind of string
you find in a database like `'America/New_York'`, `'Europe/London'`, or
`'Asia/Tokyo'`.

This tells the calendar which time zone to use for all wall-clock math: when
does a day start, what time does an event appear at, and so on.

```ts
createLuxonLocalizer({ timeZone: 'America/Chicago' })
```

**Default (when omitted):** resolved in this order:
1. `Intl.Locale.getTimeZones()` — but **only when it returns exactly one entry**. This covers locales whose region unambiguously implies a single time zone (e.g. `'en-GB'` → `Europe/London`, `'ja-JP'` → `Asia/Tokyo`). Locales that span many zones (like `'en-US'`) have more than one entry and fall through to step 2, so you never silently end up in an arbitrary zone.
2. `Intl.DateTimeFormat().resolvedOptions().timeZone` — the browser or host's current time zone.

When you supply `timeZone` explicitly it always wins, regardless of locale.

> **Tip for server-side rendering:** browser defaults are `undefined` on the
> server. Always supply an explicit `timeZone` (and usually an explicit `locale`)
> when rendering on the server so the output matches what the client will render.

---

### `output`

Controls the format of date strings the localizer _writes back out_ after doing
math on them. This affects the strings you receive in callbacks like
`onEventDrop`, and the strings stored internally.

<table>
  <thead>
    <tr>
      <th>Value</th>
      <th>What it looks like</th>
      <th>When to use it</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>'utc'</code> (default)</td>
      <td><code>2026-07-03T16:00:00Z</code></td>
      <td>Most apps. UTC is unambiguous and easy to store.</td>
    </tr>
    <tr>
      <td><code>'offset'</code></td>
      <td><code>2026-07-03T12:00:00-04:00</code></td>
      <td>When you need the local wall-clock offset preserved in the string.</td>
    </tr>
  </tbody>
</table>

```ts
createLuxonLocalizer({ timeZone: 'America/New_York', output: 'offset' })
// strings this localizer writes will look like: 2026-07-03T12:00:00-04:00
```

---

### `extendedZone`

When `true`, the localizer appends the IANA time zone name in square brackets
at the end of every date string it writes. This is the
[RFC 9557](https://www.rfc-editor.org/rfc/rfc9557) format (sometimes called
"bracketed IANA notation").

```ts
createLuxonLocalizer({ timeZone: 'America/New_York', extendedZone: true })
// strings look like: 2026-07-03T16:00:00Z[America/New_York]
```

This format preserves the intended time zone even when the UTC offset is
ambiguous (e.g. during a daylight saving transition). It is useful when you
store dates in a database and want to round-trip the time zone without loss.

**Default:** `false`.

---

### `firstDayOfWeek`

Which day the calendar week starts on. Uses ISO-8601 numbering: `1` = Monday,
`7` = Sunday.

```ts
// Start the week on Monday (common in Europe)
createLuxonLocalizer({ firstDayOfWeek: 1 })

// Start the week on Sunday (common in North America)
createLuxonLocalizer({ firstDayOfWeek: 7 })
```

**Default:** derived from your `locale`'s regional week data. For `'en-US'` that
is Sunday (`7`); for `'en-GB'` it is Monday (`1`).

---

### `formats`

An optional map letting you override the `Intl.DateTimeFormat` option sets used
for specific display roles. You only need to provide the roles you want to
change — the rest keep their built-in defaults.

The available roles are:

<table>
  <thead>
    <tr>
      <th>Role</th>
      <th>Used for</th>
      <th>Default options</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>date</code></td>
      <td>Generic date display</td>
      <td><code>{'{ year: \'numeric\', month: \'numeric\', day: \'numeric\' }'}</code></td>
    </tr>
    <tr>
      <td><code>time</code></td>
      <td>Generic time display</td>
      <td><code>{'{ hour: \'numeric\', minute: \'2-digit\' }'}</code></td>
    </tr>
    <tr>
      <td><code>datetime</code></td>
      <td>Date + time together</td>
      <td>year, month, day, hour, minute</td>
    </tr>
    <tr>
      <td><code>weekday</code></td>
      <td>Full weekday name</td>
      <td><code>{'{ weekday: \'long\' }'}</code></td>
    </tr>
    <tr>
      <td><code>weekdayShort</code></td>
      <td>Abbreviated weekday</td>
      <td><code>{'{ weekday: \'short\' }'}</code></td>
    </tr>
    <tr>
      <td><code>dayOfMonth</code></td>
      <td>Just the day number</td>
      <td><code>{'{ day: \'numeric\' }'}</code></td>
    </tr>
    <tr>
      <td><code>monthDay</code></td>
      <td>Month + day</td>
      <td><code>{'{ month: \'long\', day: \'numeric\' }'}</code></td>
    </tr>
    <tr>
      <td><code>monthYear</code></td>
      <td>Month + year</td>
      <td><code>{'{ month: \'long\', year: \'numeric\' }'}</code></td>
    </tr>
    <tr>
      <td><code>monthHeader</code></td>
      <td>Month view toolbar heading</td>
      <td><code>{'{ month: \'long\', year: \'numeric\' }'}</code></td>
    </tr>
    <tr>
      <td><code>dayHeader</code></td>
      <td>Day view toolbar heading</td>
      <td>weekday, month, day</td>
    </tr>
    <tr>
      <td><code>dayColumnHeader</code></td>
      <td>Per-column heading in week view</td>
      <td><code>{'{ weekday: \'short\', day: \'2-digit\' }'}</code></td>
    </tr>
    <tr>
      <td><code>agendaDate</code></td>
      <td>Date column in agenda view</td>
      <td>weekday short, month short, day</td>
    </tr>
    <tr>
      <td><code>agendaTime</code></td>
      <td>Time column in agenda view</td>
      <td><code>{'{ hour: \'numeric\', minute: \'2-digit\' }'}</code></td>
    </tr>
    <tr>
      <td><code>timeGutter</code></td>
      <td>Left-hand time labels in time grid</td>
      <td><code>{'{ hour: \'numeric\', minute: \'2-digit\' }'}</code></td>
    </tr>
    <tr>
      <td><code>selectRange</code></td>
      <td>Tooltip shown while dragging a selection</td>
      <td><code>{'{ hour: \'numeric\', minute: \'2-digit\' }'}</code></td>
    </tr>
  </tbody>
</table>

Each value is a plain `Intl.DateTimeFormat` options object. For example, to show
24-hour time in the time gutter:

```ts
createLuxonLocalizer({
  locale: 'en-US',
  timeZone: 'America/New_York',
  formats: {
    timeGutter: { hour: '2-digit', minute: '2-digit', hour12: false },
    agendaTime: { hour: '2-digit', minute: '2-digit', hour12: false },
  },
})
```

---

## Which localizer should I pick?

<table>
  <thead>
    <tr>
      <th>Situation</th>
      <th>Use</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Starting fresh with no date library</td>
      <td><code>localizer-temporal</code></td>
    </tr>
    <tr>
      <td>Project already uses Luxon</td>
      <td><code>localizer-luxon</code></td>
    </tr>
    <tr>
      <td>Need to support very old browsers where polyfill size matters</td>
      <td>Either — both polyfill only what is needed</td>
    </tr>
    <tr>
      <td>Storing dates in a database and need the time zone in the string</td>
      <td>Either, with <code>extendedZone: true</code></td>
    </tr>
    <tr>
      <td>Need to build a custom integration with a different date library</td>
      <td><a href="/?path=/docs/core-custom-localizer--docs">Write a custom localizer</a></td>
    </tr>
  </tbody>
</table>

---

## Input: what strings the localizer accepts

The localizer is flexible about what it reads in. All of these are valid inputs:

```
2026-07-03                        ← date only (treated as midnight in your timeZone)
2026-07-03T16:00:00Z              ← UTC instant
2026-07-03T12:00:00-04:00         ← instant with explicit offset
2026-07-03T16:00:00Z[America/New_York]  ← RFC 9557 with IANA bracket
```

All four represent the same moment in time (noon in New York, 4 PM UTC, on July
3rd 2026). The localizer normalizes any of these to its configured timeZone on
the way in.

---

## Output: what strings the localizer writes

What you get back depends on the `output` and `extendedZone` settings you
chose:

<table>
  <thead>
    <tr>
      <th><code>output</code></th>
      <th><code>extendedZone</code></th>
      <th>Example output</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>'utc'</code> (default)</td>
      <td><code>false</code> (default)</td>
      <td><code>2026-07-03T16:00:00Z</code></td>
    </tr>
    <tr>
      <td><code>'utc'</code></td>
      <td><code>true</code></td>
      <td><code>2026-07-03T16:00:00Z[America/New_York]</code></td>
    </tr>
    <tr>
      <td><code>'offset'</code></td>
      <td><code>false</code></td>
      <td><code>2026-07-03T12:00:00-04:00</code></td>
    </tr>
    <tr>
      <td><code>'offset'</code></td>
      <td><code>true</code></td>
      <td><code>2026-07-03T12:00:00-04:00[America/New_York]</code></td>
    </tr>
  </tbody>
</table>

---

## Localizer methods

Every localizer instance is available at `store.localizer` inside the calendar
store. If you are building a custom view or component using `useCalendarContext()`,
you can pull it out and use any of the methods below to do your own date math
with the same engine the calendar uses internally.

```ts
const { store } = useCalendarContext()
const { localizer } = store

// Format a date for display
localizer.format({ value: '2026-07-03T16:00:00Z', format: 'time' })
// → "12:00 PM"  (in America/New_York)
```

Many methods accept a `unit` argument. The valid unit strings are:
`'year'`, `'month'`, `'week'`, `'day'`, `'hour'`, `'minute'`, `'second'`, `'millisecond'`.

---

### Properties

These read-only values tell you how the localizer was configured.

<table>
  <thead><tr><th>Property</th><th>Type</th><th>Description / Example</th></tr></thead>
  <tbody>
    <tr>
      <td><code>locale</code></td>
      <td><code>Intl.Locale</code></td>
      <td>The resolved locale object. <code>localizer.locale.toString() // "en-US"</code></td>
    </tr>
    <tr>
      <td><code>language</code></td>
      <td><code>string</code></td>
      <td>Primary language subtag of the resolved locale. <code>localizer.language // "en"</code></td>
    </tr>
    <tr>
      <td><code>direction</code></td>
      <td><code>'ltr' | 'rtl'</code></td>
      <td>
        Writing direction implied by the locale. Uses <code>Intl.Locale.textInfo</code> where
        available, with a known-RTL language list as a fallback.<br />
        <code>localizer.direction // "rtl"</code> (for <code>locale: 'ar'</code>)
      </td>
    </tr>
    <tr>
      <td><code>timeZone</code></td>
      <td><code>string</code></td>
      <td>The configured IANA time zone. <code>localizer.timeZone // "America/New_York"</code></td>
    </tr>
    <tr>
      <td><code>extendedZone</code></td>
      <td><code>boolean</code></td>
      <td>Whether RFC 9557 bracket notation is enabled. <code>localizer.extendedZone // false</code></td>
    </tr>
    <tr>
      <td><code>output</code></td>
      <td><code>'utc' | 'offset'</code></td>
      <td>How the localizer serializes strings it writes. <code>localizer.output // "utc"</code></td>
    </tr>
  </tbody>
</table>

---

### Formatting

Use these to turn a date string into something a person can read. `format` is
either a named role string (like `'time'` or `'date'` — see the `formats`
config option above for the full list) or a plain `Intl.DateTimeFormat` options
object.

<table>
  <thead><tr><th>Method</th><th>Returns</th><th>Description / Example</th></tr></thead>
  <tbody>
    <tr>
      <td><code>format(&#123; value, format &#125;)</code></td>
      <td><code>string</code></td>
      <td>
        Format a single date for display.<br />
        <code>localizer.format(&#123; value: '2026-07-03T16:00:00Z', format: 'time' &#125;)</code><br />
        <code>// "12:00 PM"</code>
      </td>
    </tr>
    <tr>
      <td><code>formatDateRange(&#123; start, end, format &#125;)</code></td>
      <td><code>string</code></td>
      <td>
        Format a date span via <code>Intl.DateTimeFormat.formatRange</code>. The browser
        collapses repeated parts automatically ("Jun 1 – 7" instead of "Jun 1 – Jun 7").
        Use for date spans — not time-only displays.<br />
        <code>localizer.formatDateRange(&#123; start: '2026-07-01', end: '2026-07-07', format: 'monthDay' &#125;)</code><br />
        <code>// "Jul 1 – 7"</code>
      </td>
    </tr>
    <tr>
      <td><code>formatTimeRange(&#123; start, end, format &#125;)</code></td>
      <td><code>string</code></td>
      <td>
        Format two times joined by " – ". Use for time-only displays where
        <code>hour: '2-digit'</code> alignment matters.<br />
        <code>localizer.formatTimeRange(&#123; start: '...T09:00:00Z', end: '...T10:30:00Z', format: 'time' &#125;)</code><br />
        <code>// "09:00 – 10:30 AM"</code>
      </td>
    </tr>
  </tbody>
</table>

---

### Comparison

All comparison methods accept `&#123; a, b &#125;` where `a` and `b` are date strings.
When an optional `unit` is supplied, both strings are floored to that unit before
comparing — so `eq(&#123; a, b, unit: 'day' &#125;)` returns `true` as long as both fall on
the same calendar day, regardless of the time.

<table>
  <thead><tr><th>Method</th><th>Returns</th><th>Description / Example</th></tr></thead>
  <tbody>
    <tr>
      <td><code>lt(&#123; a, b &#125;)</code></td>
      <td><code>boolean</code></td>
      <td>
        <code>a</code> is before <code>b</code>.<br />
        <code>localizer.lt(&#123; a: '2026-07-01', b: '2026-07-03' &#125;) // true</code>
      </td>
    </tr>
    <tr>
      <td><code>lte(&#123; a, b &#125;)</code></td>
      <td><code>boolean</code></td>
      <td><code>a</code> is before or the same as <code>b</code>.</td>
    </tr>
    <tr>
      <td><code>gt(&#123; a, b &#125;)</code></td>
      <td><code>boolean</code></td>
      <td><code>a</code> is after <code>b</code>.</td>
    </tr>
    <tr>
      <td><code>gte(&#123; a, b &#125;)</code></td>
      <td><code>boolean</code></td>
      <td><code>a</code> is after or the same as <code>b</code>.</td>
    </tr>
    <tr>
      <td><code>eq(&#123; a, b, unit? &#125;)</code></td>
      <td><code>boolean</code></td>
      <td>
        <code>a</code> equals <code>b</code>. With <code>unit: 'day'</code>, true if both
        land on the same calendar day.<br />
        <code>localizer.eq(&#123; a: '2026-07-03T09:00:00Z', b: '2026-07-03T22:00:00Z', unit: 'day' &#125;) // true</code>
      </td>
    </tr>
    <tr>
      <td><code>neq(&#123; a, b &#125;)</code></td>
      <td><code>boolean</code></td>
      <td><code>a</code> does not equal <code>b</code>.</td>
    </tr>
    <tr>
      <td><code>inRange(&#123; value, min, max, unit? &#125;)</code></td>
      <td><code>boolean</code></td>
      <td>
        <code>value</code> falls between <code>min</code> and <code>max</code> (inclusive).<br />
        <code>localizer.inRange(&#123; value: '2026-07-03', min: '2026-07-01', max: '2026-07-07', unit: 'day' &#125;) // true</code>
      </td>
    </tr>
  </tbody>
</table>

---

### Math

Use these to move dates around, find boundaries, and measure distances.

<table>
  <thead><tr><th>Method</th><th>Returns</th><th>Description / Example</th></tr></thead>
  <tbody>
    <tr>
      <td><code>add(&#123; value, amount, unit &#125;)</code></td>
      <td><code>string</code></td>
      <td>
        Add <code>amount</code> of <code>unit</code> to <code>value</code>. Use a negative
        number to subtract.<br />
        <code>localizer.add(&#123; value: '2026-07-03', amount: 7, unit: 'day' &#125;) // "2026-07-10T..."</code>
      </td>
    </tr>
    <tr>
      <td><code>startOf(&#123; value, unit &#125;)</code></td>
      <td><code>string</code></td>
      <td>
        Floor to the start of <code>unit</code>.<br />
        <code>localizer.startOf(&#123; value: '2026-07-03T14:30:00Z', unit: 'day' &#125;) // "2026-07-03T04:00:00Z"</code> (midnight in NYC)
      </td>
    </tr>
    <tr>
      <td><code>endOf(&#123; value, unit &#125;)</code></td>
      <td><code>string</code></td>
      <td>
        Advance to the end of <code>unit</code> (the last millisecond of that period).<br />
        <code>localizer.endOf(&#123; value: '2026-07-03T14:30:00Z', unit: 'day' &#125;) // "2026-07-04T03:59:59.999Z"</code> (end of day in NYC)
      </td>
    </tr>
    <tr>
      <td><code>ceil(&#123; value, unit &#125;)</code></td>
      <td><code>string</code></td>
      <td>
        Round up to the next <code>unit</code> boundary. Returns the value unchanged if it already
        falls exactly on a boundary.<br />
        <code>localizer.ceil(&#123; value: '...T09:15:00Z', unit: 'hour' &#125;) // "...T10:00:00Z"</code>
      </td>
    </tr>
    <tr>
      <td><code>diff(&#123; a, b, unit &#125;)</code></td>
      <td><code>number</code></td>
      <td>
        <code>a − b</code>, measured in <code>unit</code>, truncated toward zero.<br />
        <code>localizer.diff(&#123; a: '2026-07-10', b: '2026-07-03', unit: 'day' &#125;) // 7</code>
      </td>
    </tr>
    <tr>
      <td><code>range(&#123; start, end, unit? &#125;)</code></td>
      <td><code>string[]</code></td>
      <td>
        All step-boundary instants from <code>start</code> through <code>end</code>.
        Default unit is <code>'day'</code>.<br />
        <code>localizer.range(&#123; start: '2026-07-01', end: '2026-07-03' &#125;) // ["2026-07-01...", "2026-07-02...", "2026-07-03..."]</code>
      </td>
    </tr>
    <tr>
      <td><code>min(&#123; values &#125;)</code></td>
      <td><code>string</code></td>
      <td>
        The earliest date in the array.<br />
        <code>localizer.min(&#123; values: ['2026-07-10', '2026-07-03', '2026-07-07'] &#125;) // "2026-07-03..."</code>
      </td>
    </tr>
    <tr>
      <td><code>max(&#123; values &#125;)</code></td>
      <td><code>string</code></td>
      <td>
        The latest date in the array.<br />
        <code>localizer.max(&#123; values: ['2026-07-10', '2026-07-03', '2026-07-07'] &#125;) // "2026-07-10..."</code>
      </td>
    </tr>
  </tbody>
</table>

---

### Week helpers

<table>
  <thead><tr><th>Method</th><th>Returns</th><th>Description / Example</th></tr></thead>
  <tbody>
    <tr>
      <td><code>firstDayOfWeek()</code></td>
      <td><code>number</code></td>
      <td>
        The configured first day of the week: <code>1</code> = Monday, <code>7</code> = Sunday.<br />
        <code>localizer.firstDayOfWeek() // 7</code> (US default)
      </td>
    </tr>
    <tr>
      <td><code>startOfWeek(value)</code></td>
      <td><code>string</code></td>
      <td>
        The first day of the week that contains <code>value</code>.<br />
        <code>localizer.startOfWeek('2026-07-03') // "2026-06-28..."</code> (prior Sunday in a Sunday-start locale)
      </td>
    </tr>
    <tr>
      <td><code>firstVisibleDay(value)</code></td>
      <td><code>string</code></td>
      <td>
        The first day shown on the month grid that contains <code>value</code> — the
        Monday or Sunday that starts the first displayed week row, even if it falls
        in the previous month.<br />
        <code>localizer.firstVisibleDay('2026-07-15') // "2026-06-28..."</code>
      </td>
    </tr>
    <tr>
      <td><code>lastVisibleDay(value)</code></td>
      <td><code>string</code></td>
      <td>
        The last day shown on the month grid — the Saturday or Sunday that ends the
        last displayed week row, even if it falls in the next month.<br />
        <code>localizer.lastVisibleDay('2026-07-15') // "2026-08-01..."</code>
      </td>
    </tr>
    <tr>
      <td><code>visibleDays(value)</code></td>
      <td><code>string[]</code></td>
      <td>
        All days shown on the month grid — from <code>firstVisibleDay</code> through
        <code>lastVisibleDay</code>. Typically 28–42 entries.<br />
        <code>localizer.visibleDays('2026-07-15') // ["2026-06-28...", ..., "2026-08-01..."]</code>
      </td>
    </tr>
  </tbody>
</table>

---

### Slot math

These are primarily used by the time-grid engine internally, but are available to
custom view builders that need to work with the same slot coordinate system.

<table>
  <thead><tr><th>Method</th><th>Returns</th><th>Description / Example</th></tr></thead>
  <tbody>
    <tr>
      <td><code>minutes(value)</code></td>
      <td><code>number</code></td>
      <td>
        The minute component (0–59) of <code>value</code> in the configured time zone.<br />
        <code>localizer.minutes('2026-07-03T09:45:00Z') // 45</code>
      </td>
    </tr>
    <tr>
      <td><code>getMinutesFromMidnight(value)</code></td>
      <td><code>number</code></td>
      <td>
        Wall-clock minutes elapsed since midnight in the configured time zone.<br />
        <code>localizer.getMinutesFromMidnight('2026-07-03T13:30:00Z') // 570</code> (9:30 AM = 9×60+30 in NYC)
      </td>
    </tr>
    <tr>
      <td><code>getTotalMin(&#123; start, end &#125;)</code></td>
      <td><code>number</code></td>
      <td>
        Real elapsed minutes between two instants (based on epoch time, not wall clock).<br />
        <code>localizer.getTotalMin(&#123; start: '...T09:00:00Z', end: '...T10:30:00Z' &#125;) // 90</code>
      </td>
    </tr>
    <tr>
      <td><code>getSlotDate(&#123; date, minutesFromMidnight &#125;)</code></td>
      <td><code>string</code></td>
      <td>
        The instant that corresponds to <code>minutesFromMidnight</code> wall-clock minutes
        into the day of <code>date</code>, accounting for DST. Each call is rebuilt from midnight
        rather than added cumulatively, so slot boundaries stay correct across a DST transition.<br />
        <code>localizer.getSlotDate(&#123; date: '2026-07-03', minutesFromMidnight: 570 &#125;) // "2026-07-03T13:30:00Z"</code> (9:30 AM in NYC)
      </td>
    </tr>
    <tr>
      <td><code>daySpan(&#123; start, end &#125;)</code></td>
      <td><code>number</code></td>
      <td>
        Whole calendar days from <code>start</code> to <code>end</code>.<br />
        <code>localizer.daySpan(&#123; start: '2026-07-01', end: '2026-07-08' &#125;) // 7</code>
      </td>
    </tr>
  </tbody>
</table>

---

### Timezone / DST

<table>
  <thead><tr><th>Method</th><th>Returns</th><th>Description / Example</th></tr></thead>
  <tbody>
    <tr>
      <td><code>getTimezoneOffset(value)</code></td>
      <td><code>number</code></td>
      <td>
        Minutes behind UTC at the instant of <code>value</code>, following the same
        sign convention as JavaScript's <code>Date.getTimezoneOffset()</code>: positive
        west of UTC, negative east.<br />
        <code>localizer.getTimezoneOffset('2026-07-03T16:00:00Z') // 240</code> (America/New_York in EDT = UTC-4)
      </td>
    </tr>
    <tr>
      <td><code>getDstOffset(&#123; start, end &#125;)</code></td>
      <td><code>number</code></td>
      <td>
        Change in UTC offset between <code>start</code> and <code>end</code>. Positive on a
        spring-forward transition (+60 when clocks jump one hour forward); negative on a
        fall-back transition (-60 when clocks fall back). Zero on any normal pair of instants.<br />
        <code>localizer.getDstOffset(&#123; start: winterInstant, end: summerInstant &#125;) // 60</code>
      </td>
    </tr>
  </tbody>
</table>

---

### Event helpers

<table>
  <thead><tr><th>Method</th><th>Returns</th><th>Description / Example</th></tr></thead>
  <tbody>
    <tr>
      <td><code>isSameDate(&#123; a, b &#125;)</code></td>
      <td><code>boolean</code></td>
      <td>
        Both instants fall on the same calendar day in the configured time zone.<br />
        <code>localizer.isSameDate(&#123; a: '2026-07-03T09:00:00Z', b: '2026-07-03T23:00:00Z' &#125;) // true</code>
      </td>
    </tr>
    <tr>
      <td><code>startAndEndAreDateOnly(&#123; start, end &#125;)</code></td>
      <td><code>boolean</code></td>
      <td>
        Both <code>start</code> and <code>end</code> are exactly at midnight (wall clock),
        which indicates a date-only all-day event rather than a timed one.<br />
        <code>localizer.startAndEndAreDateOnly(&#123; start: '2026-07-03T04:00:00Z', end: '2026-07-04T04:00:00Z' &#125;) // true</code> (midnight-to-midnight in NYC)
      </td>
    </tr>
    <tr>
      <td><code>continuesPrior(&#123; eventStart, rangeStart &#125;)</code></td>
      <td><code>boolean</code></td>
      <td>
        The event started before the visible range (it continues in from a prior period).<br />
        <code>localizer.continuesPrior(&#123; eventStart: '2026-06-30', rangeStart: '2026-07-01' &#125;) // true</code>
      </td>
    </tr>
    <tr>
      <td><code>continuesAfter(&#123; eventEnd, rangeEnd &#125;)</code></td>
      <td><code>boolean</code></td>
      <td>
        The event ends after the visible range (it continues into a future period).<br />
        <code>localizer.continuesAfter(&#123; eventEnd: '2026-07-08', rangeEnd: '2026-07-07' &#125;) // true</code>
      </td>
    </tr>
    <tr>
      <td><code>inEventRange(&#123; event, range &#125;)</code></td>
      <td><code>boolean</code></td>
      <td>
        The event overlaps with <code>range</code> at all. A zero-length event is included
        when it lands within <code>[range.start, range.end)</code>.<br />
        <code>localizer.inEventRange(&#123; event: &#123; start: '2026-07-03', end: '2026-07-05' &#125;, range: &#123; start: '2026-07-01', end: '2026-07-07' &#125; &#125;) // true</code>
      </td>
    </tr>
    <tr>
      <td><code>sortEvents(&#123; events &#125;)</code></td>
      <td><code>TEvent[]</code></td>
      <td>
        Sort an array of events: earliest start first, all-day events before timed ones
        on the same day, longer events before shorter ones when start times tie.
        Returns a new array; the original is not modified.<br />
        <code>localizer.sortEvents(&#123; events: myEvents &#125;)</code>
      </td>
    </tr>
  </tbody>
</table>
