{
  "name": "styles",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "packages/styles/src",
  "tags": ["scope:styles"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf dist && mkdir -p dist && cp -R src/. dist/",
        "cwd": "packages/styles"
      }
    }
  }
}
