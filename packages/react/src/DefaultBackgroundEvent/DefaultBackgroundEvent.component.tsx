/**
 * Default background event content: renders nothing — the colored fill comes
 * entirely from the `.bc-bg-event` box. Replace via `components.time.backgroundEvent`
 * to add a label or other content inside the background block.
 */
function DefaultBackgroundEvent() {
  return null
}

export default DefaultBackgroundEvent
