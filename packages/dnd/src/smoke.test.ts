import { describe, expect, it } from 'vitest'
import { PACKAGE_NAME } from './index'

describe('@big-calendar/dnd', () => {
  it('is scaffolded', () => {
    expect(PACKAGE_NAME).toBe('@big-calendar/dnd')
  })
})
